{
  "name": "pos-print-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local offline print server — sends raw ESC/POS to the USB receipt printer (XPrinter XP-365).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@thiagoelg/node-printer": "^1.0.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "usb": "^2.0.0"
  }
}